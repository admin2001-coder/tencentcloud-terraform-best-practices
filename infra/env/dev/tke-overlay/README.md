# dev/tke-overlay

Example TKE stack using **overlay** networking.

- Use your `terraform.tfvars` (copy from `terraform.tfvars.example`) to set region, VPC/subnet IDs, cluster name.
- Overlay mode typically has different subnet sizing requirements than VPC-CNI.
