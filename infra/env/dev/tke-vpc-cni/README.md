# dev/tke-vpc-cni

Example TKE stack using **VPC-CNI** networking (pods consume VPC subnet IPs).

This example is intentionally minimal; confirm your pod CIDR / subnet sizing in:
- `docs/architecture/vpc-cidr-plan.md`
